<script lang="ts">
	// This route now only exists to redirect old links/bookmarks — see +page.ts.
	// The actual intermodal builder lives inline on /dashboard/loads/post,
	// selected via the "Multimodal" transport mode option.
</script>
